{
  "ver": ["gobuildinfo", "-main", "${PKGNAME}"],
  "checklatest": ["querygoproxy", "-exclude-pre", "-only-latest", "${PKGNAME}"],
  "install": ["go", "install", "${PKGNAME}@latest"],
  "update": ["go", "install", "${PKGNAME}@v${VER}"],
  "after": []
}
